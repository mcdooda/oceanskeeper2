#include "../engine.h"
#include "text.h"

namespace engine
{
    namespace graphics
    {

        Text::Text(std::string text, int size)
        {
            //m_text.setText(text);
            //m_text.setSize(size);
        }

        void Text::show(Vector2d pos)
        {
            //glMatrixMode(GL_MODELVIEW);
            //glPushMatrix();
            //glMatrixMode(GL_PROJECTION);
            //glPushMatrix();
            //m_text.SetPosition(300, 300);
            //getApplication()->Draw(m_text);
            //glMatrixMode(GL_PROJECTION);
            //glPopMatrix();
            //glMatrixMode(GL_MODELVIEW);
            //glPopMatrix();
        }

    }
}
